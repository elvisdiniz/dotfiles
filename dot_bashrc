$HOME/.local/scripts/create_bin_symlinks.sh

source "${XDG_CONFIG_HOME:-$HOME/.config}/shell/init.sh"

[[ -x "$(command -v zoxide)" ]] && eval "$(zoxide init --cmd cd bash)" || true

[[ -x "$(command -v bash)" ]] && eval "$(starship init bash)" || true

[[ -x "$(command -v fzf)" ]] && eval "$(fzf --bash)" || true

[[ -x "$(command -v brew)" ]] && eval "$(brew shellenv)" || true

